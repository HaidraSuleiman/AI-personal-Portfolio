# Required: OpenAI API key for the twin's chat model
OPENAI_API_KEY=sk-...

# Optional: Pushover credentials for contact/unknown-question notifications
PUSHOVER_USER=
PUSHOVER_TOKEN=

# Comma-separated origins allowed to call this API (add your production domain when deployed)
ALLOWED_ORIGINS=http://localhost:5173
